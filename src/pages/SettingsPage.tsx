import { AppHeader } from '../components/AppHeader';

export function SettingsPage() {
  return (
    <div className="screen">
      <AppHeader subtitle="Configuración" />
      <div className="card">
        <p>Todavía no hay opciones de configuración. Próximamente.</p>
      </div>
    </div>
  );
}
